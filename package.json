{
  "name": "better-lyrics-theme-store-api",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "secret:put": "wrangler secret put",
    "secret:list": "wrangler secret list",
    "db:init": "wrangler d1 execute themes-db --remote --file=./schema.sql",
    "db:init:local": "wrangler d1 execute themes-db --local --file=./schema.sql"
  },
  "dependencies": {
    "hono": "^4.6.0"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20241106.0",
    "typescript": "^5.6.0",
    "wrangler": "^3.91.0"
  }
}
